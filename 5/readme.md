 ### 1) Алгоритм сортировки выбором (Selection Sort)
**Алгоритм сортировки выбором (Selection Sort)** — Это базовый способ упорядочить элементы массива путём последовательного нахождения самого маленького значения среди ещё несортированных элементов и переноса его в первую позицию отсортированной области массива.

## Объяснение алгоритма:
Этот метод заключается в поиске наименьшего элемента с помощью for (int i = 0; i < size - 1; i++) среди оставшихся неотсортированных элементов массива и перемещении его в начальную позицию отсортированного участка swap(). Затем повторяем процесс для оставшейся части массива (исключив первый элемент) с помощью функции for (int j = i + 1; j < size; j++).  Процесс повторяется до тех пор, пока весь массив не окажется упорядоченным.

## Временная сложность: O(n²)

## Объяснение временной сложности:
Наличие двух циклов, внешнего и внутреннего, каждый из которых зависит от n, приводит к временной сложности O(n²).

## Пример работы алгоритма:
Исходный массив: 64 25 12 22 11 

Отсортированный массив: 11 12 22 25 64 


### 2) Алгоритм сортировки обменом (или метод пузырьковой сортировки)
**Алгоритм сортировки обменом (или метод пузырьковой сортировки)** — Это один из простейших методов сортировки массива, заключающийся в неоднократном сопоставлении рядом стоящих элементов и их перестановке в случае неверного расположения относительно друг друга.

## Объяснение алгоритма:
Основная функция bubbleSort воплощает саму механику пузырьковой сортировки. Функция printArray, используемая вспомогательно, выводит состояние массива до и после завершения процесса сортировки. Внешний цикл (for i...) контролирует число полных проходов по массиву. Так как каждый полный проход гарантирует, что наибольший элемент смещается ближе к финальной позиции, дальнейшие прохождения охватывают сокращающееся количество элементов. Внутри цикла осуществляется пошаговое сравнение парных элементов (for j...), при необходимости выполняется их замена благодаря встроенной функции swap(). Переменная swapped служит индикатором наличия замен, позволяя завершить раннее работу, если массив оказывается уже отсортированным.
## Временная сложность: O(n²)

## Объяснение временной сложности:
Наличие двух циклов, внешего и внутреннего, каждый из которых зависит от n, приводит к временнуой сложности O(n²).

## Пример работы алгоритма:
Исходный массив: 64 34 25 12 22 11 90 

Отсортированный массив: 11 12 22 25 34 64 90 

### 3) Алгоритм сортировки вставками (Insertion Sort)
**Алгоритм сортировки вставками (Insertion Sort)** - Алгоритм организации элементов массива, суть которого состоит в поэтапном расширении отсортированной зоны массива посредством помещения очередного элемента в правильную позицию среди ранее упорядоченных значений.

## Объяснение алгоритма:
Первым делом формируется массив целочисленных значений. Далее инициируется выполнение функции insertionSort, реализующей непосредственно сортировку методом вставок. Эта процедура включает два уровня обработки: внешний цикл for (int i = 1; i < size; ++i) последовательно обрабатывает элементы массива, начиная со второго, а внутренний цикл while (j >= 0 && arr[j] > key) ищет подходящую позицию текущего элемента среди ранее упорядоченных членов массива. Завершив сортировку, итоговый результат отображается функцией printArray.
## Временная сложность: O(n²)

## Объяснение временной сложности:
Наличие двух циклов, внешего и внутреннего, каждый из которых зависит от n, приводит к временной сложности O(n²).

## Пример работы алгоритма:
Исходный массив: 8 3 5 1 9

Отсортированный массив: 1 3 5 8 9 


### 4) Алгоритм сортировки слиянием (Merge Sort)
**Алгоритм сортировки слиянием (Merge Sort)** - Это высокоэффективный алгоритм сортировки, использующий стратегию "разделяй и властвуй": исходный массив последовательно делится на две равные части, каждая из которых сортируется независимо, после чего оба фрагмента сливаются в общий отсортированный массив путём попарного сравнения элементов.
## Объяснение алгоритма:
Мы начинаем с выделения базовой операции деления массива пополам, определяя середину массива. Затем с помощью функции mergeSort() мы рекурсивно выполняем сортировку сначала над левой половиной массива, потом над правой. Как только обе половинки будут отсортированы, задействуем вспомогательную функцию merge(), которая совмещает отсортированные подмассивы в единый массив с использованием циклов while. Объединение производится путём последовательного сравнения элементов обоих подмассивов с помощью оператора if и внесения меньших элементов в результирующий массив. Итоговый отсортированный массив выводим на экран с помощью специальной функции вывода массива printArray().
## Временная сложность: O(nlogn)

## Объяснение временной сложности:
ри каждом рекурсивном вызове массив расщепляется на две одинаковые по размеру части, которые затем снова соединяются. Глубина такого рекурсивного разбиения достигает величины порядка \log₂𝑛, поскольку на каждом этапе массив уменьшается вдвое. На каждом уровне рекурсии проводится операция слияния, занимающая линейное время 𝑂(𝑛). Таким образом, суммарная временная сложность всего алгоритма составляет 𝑂(𝑛\log𝑛).
## Пример работы алгоритма:
Исходный массив: 12 11 13 5 6

Отсортированный массив: 5 6 11 12 13

### 5) Алгоритм сортировки Шелла (Shell Sort)
**Алгоритм сортировки Шелла (Shell Sort)** — это улучшенная версия алгоритма вставок. Основная идея заключается в том, чтобы сначала упорядочить элементы, находящиеся далеко друг от друга, постепенно уменьшая расстояние между элементами до тех пор, пока оно не достигнет единицы (стандартная сортировка вставками).

## Объяснение алгоритма:
Начинаем с некоторого большого значения шага (gap), которое меньше размера массива. Для каждого такого шага выполняем сортировку вставками среди элементов, находящихся на расстоянии gap, с помощью цикла for. После каждой итерации значение шага уменьшается с помощью цикла for (int gap = n / 2; gap > 0; gap /= 2), и снова выполняется сортировка вставками. Когда шаг становится равным единице, алгоритм фактически превращается в обычную сортировку вставками, но теперь массив почти отсортирован, что значительно ускоряет этот последний этап. Выводим массив с помощью функции printArray().

## Временная сложность: O(n^1.25)

## Объяснение временной сложности:
После каждого прохода шаг уменьшается вдвое, начиная с половины размера массива. Сначала выполняются крупные перемещения, которые эффективно перераспределяют элементы, сокращая общую работу на последующих итерациях. Засчёт этого удается достичь среднего показателя временной сложности O(n^1.25)

## Пример работы алгоритма:
Исходный массив: 12 34 54 2 3 

Отсортированный массив: 2 3 12 34 54

### 6) Алгоритм быстрой сортировки (Quicksort)
**Алгоритм быстрой сортировки (Quicksort)** — это эффективный способ сортировки массивов и списков по принципу "разделяй и властвуй", что означает разделение проблемы на более мелкие подзадачи, решение каждой из них отдельно, а затем объединение результатов.

## Объяснение алгоритма:
Выбираем последний элемент массива в качестве опорного (pivot). Используя процедуру partition, с помощью цикла for (int j = low; j < high; j++) перемещаем все элементы, меньшие или равные опорному, слева от него, а большие элементы справа. Ставим опорный элемент на свое место в отсортированном массиве с помощью swap(). Рекурсивно (quickSort) применяем тот же алгоритм к двум оставшимся частям массива (до и после опорного элемента).

## Временная сложность: 𝑂(𝑛\log𝑛)

## Объяснение временной сложности:
Каждый раз глубина рекурсии уменьшает длину обрабатываемого массива примерно вдвое, что даёт логарифмическое число рекурсивных вызовов (\log𝑛), а на каждой глубине приходится просматривать весь массив (𝑛). Общая сложность: 𝑂(𝑛\log𝑛).

## Пример работы алгоритма:
Исходный массив: 10 7 8 9 1 5

Отсортированный массив: 1 5 7 8 9 10


### 7) Алгоритм пирамидальной сортировки (HeapSort)
**Алгоритм пирамидальной сортировки (HeapSort)** - алгоритм сортировки, суть метода которого заключается в построении бинарной кучи и последовательном извлечении элементов после, перестраивая кучу каждый раз заново, пока массив не окажется отсортированным.

## Объяснение алгоритма:
Для каждого внутреннего узла проверяется условие, что родительская вершина больше обеих дочерних вершин. Если это не так, то родитель меняется с большим ребёнком с помощью swap и рекурсивно применяется процедуру heapify для восстановленного поддерева. Строится максимальная куча с помощью цикла for (int i = n / 2 - 1; i >= 0; i--). Начиная с середины массива (так как листья сами по себе являются кучами) осуществляется проход назад к началу, применяя heapify ко всей структуре. Сортировка: каждый раз берётся верхний элемент (корень), перемещается в конец массива с помощью swap в цикле for, уменьшая длину кучи, и снова heapify, сохраняя структуру. Функция print_array выводит отсортированный массив.

## Временная сложность: O(nlogn)

## Объяснение временной сложности:
Каждый проход (извлечение вершины и перестроение) занимает O(log n) и выполняется n раз, что вызывает общую временную сложность O(nlogn).

## Пример работы алгоритма:
Исходный массив: 12 11 13 5 6 7

Отсортированный массив: 5 6 7 11 12 13


### 8) Алгоритм последовательного поиска
**Алгоритм последовательного поиска** - один из простейших методов поиска элемента в массиве, основная идея которого заключается в пошаговом просмотре элементов массива, начиная с первого, пока не встретится искомое значение, либо пока весь массив не будет пройден.

## Объяснение алгоритма:
В функции sequentialSearch с помощью цикла for (int i = 0; i < size; ++i) просматриваем каждый элемент массива, начиная с первого. С помощью if последовательно сравниваем каждый элемент массива с искомым значением target. Когда находим первый совпадающий элемент, возвращаем его индекс. Если же мы дошли до конца массива и нужного значения там нет, возвращаем специальный индикатор (-1), означающий, что элемент отсутствует.

## Временная сложность: O(n)

## Объяснение временной сложности:
Наличие одного цикла for вызывает временную сложность O(n).

## Пример работы алгоритма:
Исходный массив: 10 20 80 30 60 50

Искомый элемент: 80

Элемент найден на индексе: 2


### 9) Алгоритм бинарного поиска
**Алгоритм бинарного поиска** - алгоритм нахождения элемента в отсортированном массиве путём последовательного деления массива пополам. Его эффективность обусловлена именно тем фактом, что исходный массив упорядочен заранее.

## Объяснение алгоритма: 
Определяем начальные границы поиска: левый индекс (left) равен нулю, правый индекс (right) равен длине массива минус единица. С помощью цикла while, вычисляем средний элемент mid и сравниваем искомое значение с элементом в середине массива. Если центральный элемент равен искомому значению (if (arr[mid] == target)), мы нашли нужный элемент и возвращаем его позицию. Если центральное значение больше искомого значения, значит, искомый элемент находится слева от середины, поэтому обновляем правую границу. Если центральное значение меньше искомого значения, значит, искомый элемент находится справа от середины, поэтому обновляем левую границу. Если цикл завершился, а нужного элемента не найдено, возвращаем специальный маркер (-1).

## Временная сложность: 𝒪(\log𝑛)

## Объяснение временной сложности:
Бинарный поиск имеет логарифмическое время работы: 𝒪(\log𝑛), поскольку на каждом этапе размер рассматриваемого участка уменьшается вдвое.

## Пример работы алгоритма:
Исходный массив: 1 3 5 7 9 11

Искомый элемент: 7

Элемент найден на индексе 3

### 10) Интерполирующий поиск (Interpolation Search)
**Интерполирующий поиск (Interpolation Search)** — это усовершенствованный метод поиска в отсортированных массивах, основанный на предположении, что элементы распределяются приблизительно линейно. Его преимущество перед обычным бинарным поиском заключается в возможности быстрого перехода к нужному участку массива благодаря оценке распределения значений.

## Объяснение алгоритма:
Функция интерполирующего поиска interpolationSearch. Устанавливаются начальная (low) и конечная (high) точки диапазона поиска. Проходим по элементам с помощью цикла while. Используется формула интерполяции для нахождения примерной позиции элемента double pos = low + ((double)(high - low) / (arr[high] - arr[low])) * (x - arr[low]). С помощью if проверяем элемент: если найден элемент — возвращаем индекс. Если нет — уточняем диапазон поиска и продолжаем цикл. Поиск прекращается либо когда найдено совпадение, либо когда диапазон сужается до пустого промежутка.

## Временная сложность: 𝐎(\log\log𝐧)

## Объяснение временной сложности:
Время работы интерполирующего поиска связано с предположением, что элементы массива располагаются линейно относительно индексов. Из-за этого каждое сравнение позволяет существенно сузить область поиска. Каждый шаг приближает нас к искомому элементу быстрее, чем простое деление пополам.

## Пример работы алгоритма:
Исходный массив: 10 12 13 16 18 19 20 21

Искомый элемент: 18

Element found at position 4

### 11) Поиск Фибоначчи
**Поиск Фибоначчи** — это метод бинарного поиска элементов массива, использующий числа последовательности Фибоначчи для разделения пространства поиска. Этот алгоритм особенно эффективен, когда доступ к элементам массива осуществляется последовательно, а также при работе с большими объемами данных.

## Объяснение алгоритма:
Функция fibonacciSearch реализует поиск элемента методом Фибоначчи. С помощью цикла while (fibM < n) вычисляем минимальное число Фибоначчи, которое больше длины нашего массива. Это помогает установить границы первого интервала поиска. Затем цикл while(fibM > 1) последовательно уменьшает интервал поиска, проверяя элементы на каждом этапе. Мы перемещаем границу поиска либо вправо, либо влево в зависимости от результата сравнения элементов. После завершения цикла делается дополнительная проверка последнего возможного кандидата на совпадение.

## Временная сложность: 𝒪(\log𝑛)

## Объяснение временной сложности:
На каждом шаге размер обрабатываемого участка уменьшается вдвое. Это значит, что количество итераций пропорционально количеству раз, которое число 𝑛 можно поделить на два, пока оно не станет равным единице. Таким образом, временная сложность алгоритма становится 𝒪(\log𝑛).

## Пример работы алгоритма:
Исходный массив: 10, 22, 35, 40, 45, 50, 80, 82, 85, 90, 100

Искомый элемент: 85

Element found at position 4
